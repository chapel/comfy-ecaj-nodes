notes:
  - _ulid: 01KHDHEGRB3RQZ78GSBMKYFHG6
    content: |
      Add Qwen architecture detection pattern to _ARCH_PATTERNS in
      lib/model_loader.py (match transformer_blocks, distinguish from
      Flux double_blocks). Add Qwen-specific entries to _FILE_KEY_PREFIXES
      (e.g. model.transformer.) and _EXCLUDED_PREFIXES if needed.
      Update _normalize_key() for any Qwen-specific prefix stripping.

      Tests in tests/test_model_loader.py: add Qwen detection and
      key normalization tests.
      Covers ac-7.
    author: "@claude"
    created_at: 2026-02-14T07:39:37.099Z
  - _ulid: 01KHDJPWGX6PBH3Q86WJ8JMJ9M
    content: "Implementation complete. Added Qwen architecture detection to lib/model_loader.py:
      model.transformer. prefix in _FILE_KEY_PREFIXES, Qwen pattern in _ARCH_PATTERNS (≥60
      transformer_blocks keys). Added 9 tests covering Qwen detection (qwen_checkpoint_path,
      qwen_model_prefix_checkpoint_path fixtures), key normalization (transformer. and
      model.transformer. prefixes), VAE exclusion, and tensor retrieval. 750 tests pass, ruff
      clean."
    supersedes: null
    author: "@claude"
    created_at: 2026-02-14T08:01:39.870Z
