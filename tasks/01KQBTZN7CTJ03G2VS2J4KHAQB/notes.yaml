notes:
  - _ulid: 01KQBXZERQ2HM77W4D9CS8AHHJ
    content: >-
      Approach: 

      1. Add __ecaj_artifact_kind__, __ecaj_base_identity__, __ecaj_dependency_fingerprints__,
      __ecaj_checkpoint_components__ metadata keys.

      2. Extend build_metadata() to accept artifact_kind, base_identity, dependency_fingerprints
      params and include them.

      3. Add check_checkpoint_cache() function that validates all checkpoint-specific metadata
      fields including artifact_kind=checkpoint, component classification, base identity match,
      dependency fingerprint match, and rejects old internal-format/diffusion-only artifacts.

      4. Preserve existing check_cache() and check_full_model_cache() behavior — don't break them.

      5. Preserve non-ecaj overwrite protection (ac-9).


      Edge cases: unsupported ecaj version, missing individual metadata fields, diffusion-only key
      sets (diffusion_model/noise_augmentor/model_sampling only).


      Test plan: 8 test cases per task description, each annotated with AC refs.
    supersedes: null
    author: "@claude"
    created_at: 2026-04-29T06:14:17.111Z
  - _ulid: 01KQBYN4RF952MDJP7F4Q6P7S3
    content: "[FIX_CYCLE: 1] Review verdict: changes_requested by codex"
    supersedes: null
    author: Jacob Chapel
    created_at: 2026-04-29T06:26:07.760Z
  - _ulid: 01KQBZJ21V3M1C6YQ849FXYMBG
    content: >-
      Fix cycle 1: addressed both review blockers.

      1. Cache routing: checkpoint-style recipes (containing RecipeModel nodes) now call
      check_checkpoint_cache instead of check_full_model_cache via
      _recipe_has_checkpoint_components() detection.

      2. Metadata writing: build_metadata now receives artifact_kind='checkpoint', base_identity,
      dependency_fingerprints, and checkpoint_components=True for checkpoint-style saves.

      Added 9 new tests: 6 for _recipe_has_checkpoint_components detection, 3 for checkpoint cache
      routing and metadata writing in Exit path.

      All 1322 tests pass. kspec validate passes.
    supersedes: null
    author: "@claude"
    created_at: 2026-04-29T06:41:55.259Z
