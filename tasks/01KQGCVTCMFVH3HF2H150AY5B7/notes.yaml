notes:
  - _ulid: 01KQGDY83RF6Q2F9Q597SNNZDG
    content: >-
      Approach: existing diffusion-only save writes safetensors with bare `diffusion_model.*` keys,
      which Comfy's load_diffusion_model_state_dict cannot detect (it strips one of
      model.diffusion_model./model.model./net./model.). Plan:

      1. Diffusion-only save path remaps merged `diffusion_model.X` -> `model.diffusion_model.X`
      keys before passing manifest + writes to MaterializationSink (matches
      process_unet_state_dict_for_saving default).

      2. Add metadata fields:
         - __ecaj_artifact_kind__ stays 'diffusion'
         - __ecaj_source_model_kind__ = 'diffusion_model' (new) so cache hits validate source kind round-trip.
         - Pass through to build_metadata.
      3. _classify_temp_artifact verifies diffusion-model source key prefix
      (model.diffusion_model.*) for kind='diffusion'.

      4. New _load_diffusion_model_artifact calls comfy.sd.load_diffusion_model(save_path) for
      cache-hit + post-save returns; preserves Comfy-owned memory.

      5. check_full_model_cache learns expected_source_model_kind; manifest remap mirrors saved
      external keys.

      6. Existing ac-* (artifact-safety/round-trip, completeness, BF16 raw-byte) preserved.


      Test cases (annotated):

      - diffusion-only save writes model.diffusion_model.* keys (sdxl/flux/zimage/qwen synthetic
      state dicts).

      - diffusion save metadata stores source_model_kind='diffusion_model' and
      artifact_kind='diffusion'.

      - diffusion save calls comfy.sd.load_diffusion_model for returned MODEL (cache miss + cache
      hit).

      - cache hit rejects artifact with checkpoint source_model_kind.

      - cache hit rejects artifact with artifact_kind='checkpoint'.

      - companion-separation: artifact contains no conditioner./first_stage_model. keys.

      - noop diffusion path also writes external keys + uses load_diffusion_model.

      - BF16 streaming raw byte preservation still works after key remap.


      Keeps checkpoint branch (_execute_checkpoint_save / _noop checkpoint) unchanged. Existing
      tests using diffusion_model.* artifact keys must be updated to model.diffusion_model.* (only
      diffusion-only branch).
    supersedes: null
    author: "@claude"
    created_at: 2026-05-01T00:10:12.472Z
  - _ulid: 01KQGEXBN4YERH0VGRKN1KDCH7
    content: >-
      Implementation complete:

      - Added _to_external_diffusion_key, _comfy_load_diffusion_model,
      _load_diffusion_model_artifact in nodes/exit.py.

      - Diffusion-only branch (cache-miss + cache-hit + no-op) writes external
      model.diffusion_model.* layout via MaterializationSink, returns MODEL through
      comfy.sd.load_diffusion_model.

      - Added __ecaj_source_model_kind__ metadata; check_full_model_cache now validates it
      (build_metadata too).

      - _classify_temp_artifact rejects diffusion artifacts containing internal-format
      diffusion_model.* keys; MaterializationSink.finalize accepts a pre_publish_check callback.

      - New tests (20) in test_diffusion_model_saved_output.py cover key layout for
      SDXL/Flux/Z-Image/Qwen-style state dicts, companion separation, source-kind metadata,
      cache-rejection on mismatched source/artifact kind, BF16 raw byte preservation across remap,
      comfy loader path on save + cache hit, and _classify_temp_artifact diffusion path.

      - Updated affected existing tests for new external key layout (test_full_model_streaming.py,
      test_incremental_recompute.py) and conftest stub for comfy.sd.load_diffusion_model.


      Verification: 1606 tests pass, ruff clean, kspec validate has only pre-existing warnings. No
      checkpoint-save behavior changes.


      Live-validation harness: this task does not extend the guarded harness in scripts/manual since
      checkpoint-save validation is already covered there; a follow-up task can add a
      diffusion-model-specific guarded harness (deferred to keep scope tight to spec ACs).
    supersedes: null
    author: "@claude"
    created_at: 2026-05-01T00:27:11.908Z
  - _ulid: 01KQGF8YYABQQX3K6K5DHYPJGM
    content: "[FIX_CYCLE: 1] Review verdict: changes_requested by jacob.chapel@gmail.com"
    supersedes: null
    author: Jacob Chapel
    created_at: 2026-05-01T00:33:32.106Z
  - _ulid: 01KQGFJWFKWAPGJT3WNC4N1F36
    content: "Fix cycle 1 plan: (1) Add _resolve_diffusion_models_path that uses
      folder_paths.get_folder_paths('diffusion_models') with 'unet' fallback; add a kind-aware
      _resolve_save_path router. (2) Move is_checkpoint detection BEFORE save_path resolution in
      _execute_full_saved_model and _execute_full_model_noop, and use the router. (3) Update
      IS_CHANGED to pick the resolver based on widen recipe kind. (4) Update _run_diffusion_save
      test helper to patch the diffusion resolver instead of _resolve_checkpoints_path; add new
      tests proving diffusion saves resolve through diffusion_models folder without patching
      _resolve_checkpoints_path. (5) Extend scripts/manual/ guarded harness to support a real
      standalone diffusion-model workflow validating reload via UNETLoader/load_diffusion_model with
      separately supplied CLIP+VAE."
    supersedes: null
    author: "@claude"
    created_at: 2026-05-01T00:38:57.267Z
  - _ulid: 01KQGGAS00H3BQ8V24EHDRFBKE
    content: "Fix cycle 1 complete: addressed both review blockers. Blocker 1 (diffusion saves under
      checkpoints folder): added _resolve_diffusion_models_path + kind-aware _resolve_save_path
      router in nodes/exit.py; moved is_checkpoint detection before save-path resolution in
      _execute_full_saved_model, _execute_full_model_noop, and IS_CHANGED. Blocker 2 (no
      diffusion-model live harness): added scripts/manual/real_comfy_diffusion_model_validation.py —
      guarded harness building UNETLoader -> WIDENEntry -> WIDENExit save and downstream validation
      via UNETLoader + separate (Dual)CLIPLoader + VAELoader. Added 8 new behavioral tests covering
      the resolver/routing without patching _resolve_checkpoints_path, and 30 tests for the new
      harness (guards, workflow shape, classification, report formatting). Migrated existing
      diffusion-only tests across
      test_full_model_streaming/test_incremental_recompute/test_memory_management/test_diffusion_mo\
      del_saved_output to patch the new _resolve_save_path router. uv run pytest: 1644 passed; ruff
      clean; kspec validate has only pre-existing warnings."
    supersedes: null
    author: "@claude"
    created_at: 2026-05-01T00:52:00.128Z
  - _ulid: 01KQGGS8WYDMTJFAZQY1Z0FGM7
    content: "[FIX_CYCLE: 2] Review verdict: changes_requested by jacob.chapel@gmail.com"
    supersedes: null
    author: Jacob Chapel
    created_at: 2026-05-01T00:59:55.166Z
  - _ulid: 01KQGHBFFE4CFQR2Q027327AYC
    content: "Fix cycle 2 complete: addressed the single review blocker (diffusion live harness API
      shape). Added weight_dtype='default' to every UNETLoader node built by the diffusion
      live-validation harness — build_diffusion_save_workflow, build_diffusion_downstream_workflow,
      and build_diffusion_cache_reuse_workflow — so the generated live prompts pass current
      ComfyUI's required_input_missing validation. Added 3 new behavioral tests asserting
      weight_dtype is present on every UNETLoader node (save run, downstream reload run, cache-reuse
      run). Updated save-workflow docstring to document that both unet_name and weight_dtype are
      required and that omitting weight_dtype causes required_input_missing prompt rejection.
      Verified other ACs unchanged: diffusion saves still route through diffusion_models folder,
      diffusion artifacts still emit external model.diffusion_model.* layout with
      __ecaj_source_model_kind__='diffusion_model' metadata, cache hits still reject mismatched
      source_model_kind/artifact_kind, returned MODEL still loads via comfy.sd.load_diffusion_model.
      uv run pytest 1647 passed; uv run ruff check . clean; kspec validate has only pre-existing
      warnings."
    supersedes: null
    author: "@claude"
    created_at: 2026-05-01T01:09:51.726Z
